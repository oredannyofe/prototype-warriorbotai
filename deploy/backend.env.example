# Backend environment for production
# Copy to deploy/backend.env and set real values
SECRET_KEY=change-me-very-secret
ADMIN_SECRET=change-admin-secret
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DATA_KEY=
# CORS: set to your domain (e.g., https://warriorbot.example.com) or *
CORS_ALLOW_ORIGINS=*
